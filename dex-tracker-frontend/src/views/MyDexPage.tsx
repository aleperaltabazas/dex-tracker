import React from "react";
import { hot } from "react-hot-loader";

type MyDexPageProps = {};

const MyDexPage = (props: MyDexPageProps) => {
  return <div></div>;
};

export default hot(module)(MyDexPage);
